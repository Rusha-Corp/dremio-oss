apiVersion: v1
kind: Service
metadata:
  name: dremio-executor
  namespace: dremio
spec:
  clusterIP: None  # Headless service for pod DNS resolution
  ports:
    - port: 45678
      targetPort: 45678
      name: rpc
  selector:
    app: dremio
    role: executor
