# This HPA was replaced by KEDA ScaledObjects (10-keda-scaledobject.yaml).
# The old dremio-executor deployment no longer exists; KEDA manages
# dremio-executor-small and dremio-executor-large independently.
